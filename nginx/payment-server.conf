events {}

http {
  upstream payment_servers {
    server payment-server-1:8000;
    server payment-server-2:8000;
    server payment-server-3:8000;
  }

  server {
    listen 80;

    location / {
      proxy_pass http://payment_servers;
    }
  }
}
